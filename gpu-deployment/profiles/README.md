# GPU profiles

Internal launch profiles used by Subconscious FDEs. Customer deploy path is [gpu-deployment/README.md](../README.md): Docker Hub pull credentials and optional AWS/GCP worker routing.

These files remain until helper scripts for hyperscalers and NeoClouds replace them. Do not treat this directory as a self-serve installer.
