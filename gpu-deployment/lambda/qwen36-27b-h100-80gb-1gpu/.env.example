# qwen36-27b-h100-80gb-1gpu on Lambda: 1 x h100-80gb; 1 worker(s).
# Copy to .env. deploy.sh reads literal KEY=value assignments from that file.
# Use absolute paths: shell variables, ~, and command substitutions are not expanded.
# Nonempty process environment variables override these defaults.
# No secrets are included. Keep .env out of git.
# LAMBDA_INSTANCE_TYPE: empty means no profile-specific override.
SSH_USER=ubuntu
SSH_KEY=
SSH_PORT=22
INSTANCE_NAME=
LAMBDA_INSTANCE_TYPE=gpu_1x_h100_pcie_80gb
LAMBDA_REGION=us-south-1
LAMBDA_API_KEY=
LAMBDA_SSH_KEY_NAME=
